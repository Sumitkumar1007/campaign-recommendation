For `6 months`, this is the month split now.

**Training**
Request:
- `months = 6`

Raw Postgres fetch:
- training prep fetches `months + 1`
- so it fetches `7` source months total

Example if latest available months are:
- `JAN-2026, FEB-2026, MAR-2026, APR-2026, MAY-2026, JUN-2026, JUL-2026`

Then effective training split becomes:
- `train`: `JAN-2026, FEB-2026, MAR-2026, APR-2026`
- `validation`: `MAY-2026`
- `test`: `JUN-2026`
- `prediction candidate`: `JUL-2026`

So with `6 months`, training is effectively:
- `4 train + 1 validation + 1 test + 1 prediction month`

**Inference**
If the model bundle has:
- `history_window_months = 6`

and inference runs for:
- `source_month = JUL-2026`
- `predict_month = AUG-2026`

Then communication history fetched for inference is:
- `FEB-2026, MAR-2026, APR-2026, MAY-2026, JUN-2026, JUL-2026`

And prediction population is:
- cases for `AUG-2026`

So inference uses:
- `6 communication months for features`
- `AUG-2026` cases for prediction output

**Drift**
With the pooled full-training baseline logic you asked for, drift now should use:

Baseline:
- all training-split months pooled together
- for this example:
  - `JAN-2026, FEB-2026, MAR-2026, APR-2026`

Inference side:
- active inference source month
- for this example:
  - `JUL-2026`

So for `6 months`, drift compares:
- `baseline pooled`: `JAN-2026` to `APR-2026`
- `inference month`: `JUL-2026`

Not:
- single month baseline
- not validation month
- not test month

**Short table**

For `6 months` example:

- Raw fetch for training: `JAN` to `JUL`
- Train: `JAN` to `APR`
- Validation: `MAY`
- Test: `JUN`
- Prediction candidate in training: `JUL`
- Inference history window: `FEB` to `JUL`
- Inference active source month: `JUL`
- Inference target/predict month: `AUG`
- Drift baseline: pooled `JAN` to `APR`
- Drift comparison month: `JUL`

If you want, I can give the same breakdown for `3`, `10`, and `12` months also.